import styled from 'styled-components';

import { Line } from 'react-chartjs-2';

export const Container = styled.div`
  max-width: 800px;
  max-height: 700px;
`;

export const ContentLine = styled(Line)``;
